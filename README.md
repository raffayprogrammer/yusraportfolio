# Yusra Khan — Portfolio

AI Automation Engineer. Production-grade automation systems across document processing,
CRM workflows, voice agents and content pipelines.

Single static page, no build step and no dependencies. Open `index.html` locally, or
serve it with GitHub Pages via **Settings → Pages → Deploy from branch → main → / (root)**.
